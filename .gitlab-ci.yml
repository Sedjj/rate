stages:
  - build

deploy:
  stage: build
  #only:
  #  - production # только для этой ветки
  script:
    - docker-compose stop
    - docker-compose --build
    - docker-compose up -d
  when: always
  #except:
  #  - master # исключить ветки
  tags:
    - docker